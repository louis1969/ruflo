import type { EvalSuite } from '../types.js';

export const codingSuite: EvalSuite = {
  id:          'coding',
  name:        'Coding Benchmark',
  description: 'Tests code generation across algorithm complexity levels',
  version:     '1.0',
  cases: [
    {
      id:         'coding-fizzbuzz',
      name:       'FizzBuzz',
      taskType:   'coding',
      complexity: 'low',
      prompt:     'Write a JavaScript function called fizzBuzz(n) that returns an array of strings from 1 to n, replacing multiples of 3 with "Fizz", multiples of 5 with "Buzz", and multiples of both with "FizzBuzz".',
      criteria: [
        'Defines a function named fizzBuzz that accepts a parameter n',
        'Returns an array (not logs to console)',
        'Correctly handles multiples of 3 (Fizz), 5 (Buzz), and 15 (FizzBuzz)',
        'Uses a loop or array method to iterate from 1 to n',
        'Output is valid JavaScript syntax',
      ],
      expectedKeywords: ['function', 'return', 'fizz', 'buzz', 'for', 'array'],
      tags: ['javascript', 'algorithm', 'beginner'],
    },
    {
      id:         'coding-binary-search',
      name:       'Binary Search',
      taskType:   'coding',
      complexity: 'medium',
      prompt:     'Implement a binary search function in TypeScript: binarySearch<T>(sorted: T[], target: T, compareFn: (a: T, b: T) => number): number. Return the index of the target or -1 if not found. Include proper generic typing and handle edge cases (empty array, target not present).',
      criteria: [
        'Uses correct binary search logic (not linear scan)',
        'Uses the compareFn parameter for comparisons, not direct equality',
        'Returns -1 when target is not found',
        'Handles empty array edge case',
        'Uses TypeScript generics correctly',
        'Has O(log n) time complexity',
      ],
      expectedKeywords: ['binary', 'mid', 'left', 'right', 'compareFn', 'generic'],
      tags: ['typescript', 'algorithm', 'search'],
    },
    {
      id:         'coding-lru-cache',
      name:       'LRU Cache',
      taskType:   'coding',
      complexity: 'high',
      prompt:     'Implement an LRU (Least Recently Used) Cache class in TypeScript with O(1) get and put operations. The cache should have a configurable capacity. Include the class definition with proper typing, using a Map and a doubly-linked list internally for O(1) operations.',
      criteria: [
        'Implements get(key) and put(key, value) methods',
        'get() returns -1 or undefined for missing keys',
        'Evicts the least recently used item when capacity is exceeded',
        'Both get and put operate in O(1) time (uses Map + doubly-linked list)',
        'Capacity is respected — never exceeds configured max size',
        'TypeScript types are used correctly',
      ],
      expectedKeywords: ['class', 'map', 'capacity', 'node', 'head', 'tail', 'get', 'put'],
      tags: ['typescript', 'data-structures', 'cache'],
    },
    {
      id:         'coding-async-retry',
      name:       'Async Retry with Backoff',
      taskType:   'coding',
      complexity: 'medium',
      prompt:     'Write a TypeScript utility function: retry<T>(fn: () => Promise<T>, opts: { maxAttempts: number; backoffMs: number; jitter?: boolean }): Promise<T>. It should retry the function on failure with exponential backoff and optional jitter. Throw the last error if all attempts are exhausted.',
      criteria: [
        'Accepts an async function and retry options',
        'Implements exponential backoff (doubling delay)',
        'Supports optional jitter (randomizes backoff)',
        'Throws the final error after maxAttempts are exhausted',
        'Does not retry on success',
        'Uses proper async/await or Promise patterns',
      ],
      expectedKeywords: ['retry', 'backoff', 'attempt', 'exponential', 'jitter', 'async', 'await'],
      tags: ['typescript', 'async', 'utility'],
    },
    {
      id:         'coding-event-emitter',
      name:       'Typed Event Emitter',
      taskType:   'coding',
      complexity: 'high',
      prompt:     'Implement a strongly-typed EventEmitter class in TypeScript. It should be generic over an event map type (e.g., EventEmitter<{ click: MouseEvent; change: string }>). Include on(), off(), and emit() methods with correct type inference — listeners should receive the correct payload type for each event.',
      criteria: [
        'Uses TypeScript generics to type the event map',
        'on(event, listener) infers the correct payload type from the event name',
        'emit(event, payload) requires the correct payload type',
        'off(event, listener) removes specific listeners',
        'Multiple listeners per event are supported',
        'Calling emit with wrong payload type should be a TypeScript compile error (types enforce this)',
      ],
      expectedKeywords: ['generic', 'map', 'on', 'off', 'emit', 'listener', 'keyof'],
      tags: ['typescript', 'patterns', 'generics'],
    },
  ],
};
